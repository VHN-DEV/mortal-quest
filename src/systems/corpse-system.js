export class CorpseSystem {
    constructor(player, ui) {
        this.player = player;
        this.ui = ui;
    }

    refine(corpseId, materials) {
        // Placeholder for corpse refining logic
        return { success: true, msg: 'Tính năng Luyện Thi đang được phát triển!' };
    }
    
    update(delta) {}
}
